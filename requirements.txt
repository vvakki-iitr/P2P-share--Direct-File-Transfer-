# Note: This is a Node.js project. Dependencies are formally managed in package.json.
# To install all dependencies, run:
# cd server && npm install
# cd client && npm install

# --- Backend Dependencies (server/) ---
cors
express
nanoid
socket.io

# --- Frontend Dependencies (client/) ---
react
react-dom
socket.io-client
vite
